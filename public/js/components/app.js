import React from 'react';
import Items from './items';

var App = React.createClass({
  render: function () {
    var Route = '';

    return (
      <Route state={state}></Route>
    );
  }
});

export default App;